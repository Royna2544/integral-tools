#pragma once

#include <stdexcept>
#include <limits>

// Forward declarations
void prime();
void calc();
void combination();

// Menu navigation control
enum class MenuAction {
    Continue,
    ReturnToMain
};
